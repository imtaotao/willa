# Willa 组件路线

这个文档只记录尚未实现、尚未公开或接口仍待确认的组件方向。已经作为公开组件进入
示例站的内容不再放在路线清单中；组件创建、CSS、主题变量和示例文档规则以
[组件指南](./component.md)、[CSS 指南](./css.md) 和
[代码风格指南](./style.md) 为准。

Willa 面向 AI 产品、博客、文档站、内容平台、MDX 页面和富交互内容渲染。AI 产品是
当前新增主线，但不是替代博客和文档站目标；后续组件规划需要同时服务产品交互和
富内容表达。

## 规划原则

| 原则       | 说明                                                                                                                                                                           |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 包边界清晰 | 布局底座归 `@willa-ui/layout`；通用展示底座归 `@willa-ui/content`；通用表单底座归 `@willa-ui/form`；AI 场景组件归 `@willa-ui/ai`；外部平台和富媒体场景归 `@willa-ui/widgets`。 |
| 少而完整   | 优先建设能覆盖真实产品常用路径的组件，不为少见场景提前堆参数。                                                                                                                 |
| 可组合     | 高阶组件优先组合现有基础组件，避免把输入、布局、状态和业务流程塞进一个过重组件。                                                                                               |
| 不重复     | 已有基础组件能表达的能力不再新建同义组件；确实需要组合封装时，先明确差异。                                                                                                     |

## 当前结论

现有组件复用方向基本成立：PromptInput 复用 InputPanel，ReasoningSteps 复用
Timeline，Upload 复用 FilePreview 和 Download，说明跨包组合链路可继续沿用。
GenerationCard 和 ToolCallCard 暂不抽公共 API；折叠、状态标记和卡片 chrome
只作为 AI 包内部私有 primitive 的观察方向。

表单选择类已经通过 `useSelectablePanel`、`selectablePanelParts` 和内部
`useSelectionModel` 共享浮层、面板基础与选择状态。继续新增 Combobox、
AutoComplete 等选择组件前，应优先复用这套内部模型，覆盖选中值归一化、多选切换、
清空和 `renderValue`，避免复制 Picker / TreeSelect 的选择状态逻辑。

文件相关组件已经形成复用链路：Upload 负责选择与上传状态，AttachmentList 负责 AI
附件列表，FilePreview 负责预览与兜底展示。文件元信息、预览类型、进度和文件大小等
纯数据逻辑已经收敛到 `@willa-ui/shared` helper；渲染结构仍留在各组件内，避免跨包
UI 依赖。AudioLink / VideoLink 已共享 widgets 内部 media inline helper，保持来源
解析和内联内容结构一致。

CSS 主题归属当前基本合理。ColorPicker 色谱和 Separator 虚线渐变属于结构或功能色，
可以留在组件 CSS；新增主题变量仍必须放在组件所属包的 theme 文件中，不进入 `willa`
聚合包。

`RangeInput` 已覆盖单值滑动输入的常用能力，包括尺寸、当前值展示、格式化和轻量刻度。
后续阈值、比例、透明度、模型温度等单值场景继续复用 `RangeInput`；只有出现多滑块范围、
垂直滑动、复杂 tooltip 或自定义轨道内容等复合交互时，才重新评估独立 `Slider` /
`RangeSlider`。

已有大 API 组件需要控制继续膨胀。`Tour` 已按文案、交互行为、定位、渲染和样式类名
收敛为分组 API；后续同类组件新增能力时，优先使用稳定配置对象或组合区域，避免继续
平铺根级 props。

`Select`、`PromptInput` 这类场景预设，以及 `Alert`、`Callout`、`FormMessage`、
`Toast`、`Result` 的反馈层级边界，属于长期组件设计规则，见
[组件指南](./component.md)。404 和资源不存在页面归入 `EmptyState` recipe，不保留独立
public 组件。

## AI 产品组件

后续 AI 相关产品建设围绕“会话、上下文、反馈、模型配置、复杂结果展示”推进。
已经具备的输入、消息、附件、工具过程和生成结果组件不在本节重复列出。

### P0

| 能力       | 待补组件               | 使用场景                                             |
| ---------- | ---------------------- | ---------------------------------------------------- |
| 会话管理   | `ConversationList`     | 会话分组、搜索、重命名、删除和归档。                 |
| 反馈闭环   | `FeedbackBar`          | 对 AI 回复点赞、点踩、举报问题和补充反馈。           |
| 模板选择   | `PromptTemplatePicker` | Prompt 模板选择、最近使用、收藏和分类筛选。          |
| 模型配置   | `ModelSelector`        | 模型选择、能力标签、上下文长度、速度或质量倾向展示。 |
| 上下文管理 | `ContextPanel`         | 展示当前 AI 使用的文件、网页、文档和知识库条目。     |

### P1

| 能力       | 待补组件             | 使用场景                                                     |
| ---------- | -------------------- | ------------------------------------------------------------ |
| 上下文容量 | `ContextWindowMeter` | 展示 token、文件或上下文容量占用。                           |
| 表格结果   | `TableRenderer`      | AI 生成的 Markdown 表格、复制和导出 CSV。                    |
| 多阶段进度 | `ProgressSteps`      | 长任务阶段进度，例如理解需求、检索资料、生成方案和校验结果。 |
| Agent 轨迹 | `AgentTimeline`      | 展示 Agent 执行步骤、工具调用和状态变化。                    |
| 人工确认   | `HumanApprovalCard`  | AI 请求用户确认后继续执行。                                  |

### P2

| 能力       | 待补组件                                                            | 使用场景                                              |
| ---------- | ------------------------------------------------------------------- | ----------------------------------------------------- |
| 链路追踪   | `TraceViewer`                                                       | 推理链路、工具调用链路、token、latency 和 cost 展示。 |
| 多模态输入 | `AudioRecorder`、`VoiceInput`、`ImagePreview`、`VideoPreview`       | 语音、图片和视频输入与预览。                          |
| 图像标注   | `BoundingBoxOverlay`、`OCRResultViewer`                             | 目标检测框和 OCR 结果展示。                           |
| 企业治理   | `PermissionMatrix`、`AuditLogViewer`、`QuotaUsage`、`CostEstimator` | 权限、审计、额度和成本管理。                          |
| 工作流编排 | `WorkflowCanvas`、`NodeCard`、`EdgeConnector`                       | Agent 或自动化流程的可视化编排。                      |

## 基础产品组件

基础组件路线只记录还缺的通用产品能力。已经公开的表单、表格、弹层、反馈、文件、
上传下载、代码、引用和内容渲染组件不在这里重复列出。

布局能力优先建设低耦合底座，再考虑可拖拽、可调整尺寸的复杂面板。分栏拖拽类组件
依赖最小宽度、持久化、键盘可访问性、移动端降级和嵌套策略，不能作为布局体系的第一
层抽象。

基础组件缺口参考 Ant Design、MUI 和 Radix Primitives 的通用组件分类，但只保留
符合 Willa 当前定位的高频产品能力。组件落点遵循现有包边界：展示、反馈和导航类放在
`@willa-ui/content`，表单输入类放在 `@willa-ui/form`，布局底座放在
`@willa-ui/layout`。

### P0

| 能力     | 待补组件                   | 包归属              | 使用场景                                                             |
| -------- | -------------------------- | ------------------- | -------------------------------------------------------------------- |
| 标签展示 | `Tag` / `Chip`             | `@willa-ui/content` | 分类、状态、筛选项、可关闭标签和轻量选中态。                         |
| 操作菜单 | `Dropdown` / `ContextMenu` | `@willa-ui/content` | 表格行操作、工具栏菜单、右键菜单和更多操作入口。                     |
| 分段切换 | `Segmented`                | `@willa-ui/content` | 示例页能力切换、视图切换、筛选维度切换和轻量模式切换。               |
| 折叠内容 | `Accordion` / `Collapse`   | `@willa-ui/content` | FAQ、配置面板、详情折叠和文档内容分组；区别于内容型 `DetailsBlock`。 |

`Segmented` 是轻量单选分段控件，优先覆盖视图、尺寸、模式和筛选维度切换。
`ToggleGroup` 暂不作为 P0 独立公共组件；工具栏里的开关态优先由 `Toolbar` 组合
`Button` / `IconButton` 的 `pressed` 状态承载，只有出现稳定的多选或单选工具状态组
需求时再重新评估。

### P1

| 能力     | 待补组件                       | 包归属              | 使用场景                                                         |
| -------- | ------------------------------ | ------------------- | ---------------------------------------------------------------- |
| 联想选择 | `AutoComplete` / `Combobox`    | `@willa-ui/form`    | 搜索建议、命令选择、远程数据选择和可输入选择器。                 |
| 确认操作 | `ConfirmDialog` / `Popconfirm` | `@willa-ui/content` | 删除、发布、覆盖等危险操作确认；优先组合 `Dialog` 或 `Popover`。 |

### P2

| 能力     | 待补组件     | 包归属           | 使用场景                                                   |
| -------- | ------------ | ---------------- | ---------------------------------------------------------- |
| 时间选择 | `TimePicker` | `@willa-ui/form` | 时间点、周期和调度配置；日期时间套件稳定前保持轻量接口。   |
| 级联选择 | `Cascader`   | `@willa-ui/form` | 多层级分类选择；已有 `TreeSelect` 能覆盖时不新建同义能力。 |
| 提及输入 | `Mentions`   | `@willa-ui/form` | 评论、协作和编辑器中的用户、资源或变量提及。               |

### 基础设施

| 优先级 | 能力         | 当前状态                                                                                                 | 待补模块              | 包归属              | 下一步                                                                         |
| ------ | ------------ | -------------------------------------------------------------------------------------------------------- | --------------------- | ------------------- | ------------------------------------------------------------------------------ |
| P1     | 浮层基础     | `@willa-ui/form` 已有内部 `useFloatingPanel`，服务 DatePicker、Select 等                                 | `useFloatingPanel`    | `@willa-ui/shared`  | 提取前确认触发器定位、窗口边界、关闭行为、portal 交互和表单组件兼容性。        |
| P1     | 选择模型     | Picker / TreeSelect 已共享内部 `useSelectionModel`，覆盖选中值归一化、多选切换、清空和展示值             | 存量复用              | `@willa-ui/form`    | 增加 Combobox / AutoComplete 时继续复用内部选择状态模型，暂不暴露公共 API。    |
| P1     | 文件项模型   | Upload、AttachmentList 和 FilePreview 已共享文件元信息、预览模式、状态、类型、进度和大小格式化 helper    | 存量复用              | `@willa-ui/shared`  | 后续文件组件继续复用纯数据 helper；渲染结构仍留在各组件，避免跨包 UI 依赖。    |
| P1     | 媒体内联模型 | AudioLink / VideoLink 已共享 widgets 内部 media inline helper，收敛来源解析和内联内容结构                | 存量复用              | `@willa-ui/widgets` | 保持组件 API 独立；只有出现第三个同类媒体链接组件时再评估公共抽象。            |
| P1     | 受控状态     | `@willa-ui/shared` 已有 `useControllableState`                                                           | 组件迁移              | 各组件包            | 先覆盖 `value` / `defaultValue` / `onChange` 重复实现，再逐步迁移组件。        |
| P2     | AI 卡片      | GenerationCard 和 ToolCallCard 都有折叠、状态标记和卡片 chrome，但语义和信息结构仍不同                   | AI 内部私有 primitive | `@willa-ui/ai`      | 先观察重复是否继续扩大；只有在语义稳定后才抽私有基础部件，不提前抽成公共组件。 |
| P2     | 复制能力     | `@willa-ui/shared` 已有 `copyToClipboard` 和 `useCopyToClipboard`，`@willa-ui/content` 已有 `CopyButton` | 更多存量迁移          | 各组件包            | 保留现有工具、hook 和按钮语义，继续迁移剩余复制入口。                          |

## 内容与渲染增强

内容组件仍是 Willa 的重要目标。后续增强应优先补齐博客、文档站、MDX 和内容平台中
可复用的能力，而不是只服务单一示例。

| 优先级 | 待补能力   | 说明                                           |
| ------ | ---------- | ---------------------------------------------- |
| P1     | 资源索引   | 面向文档站和知识库的资源列表、分类和搜索组合。 |
| P2     | 富媒体编排 | 多媒体内容的分组、说明、来源和版权信息组织。   |

## 暂缓建设

以下方向需要产品形态更稳定后再设计接口：

| 方向                          | 原因                                                                                            |
| ----------------------------- | ----------------------------------------------------------------------------------------------- |
| `Transfer` 穿梭框             | 更偏企业后台批量选择，当前可由表格、列表和筛选组合覆盖。                                        |
| 独立 `Slider` / `RangeSlider` | `RangeInput` 已覆盖单值滑动输入；多滑块范围、垂直滑动或复杂自定义轨道出现稳定场景后再独立设计。 |
| 独立 `MarkdownRenderer`       | 先评估复用现有 MDX、代码和内容组件，避免重复建设渲染管线。                                      |
| 完整工作流编辑器              | 容易演化成平台级系统，需要等 Agent 产品形态稳定后再拆分通用组件。                               |
| 完整企业治理后台              | 权限、审计、额度和成本管理更偏平台配置，通用 UI 抽象需要真实产品验证。                          |
