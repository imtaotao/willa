{
  "name": "willa-workspace",
  "version": "0.0.1-alpha.8d8fe96",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "chentao.arthur",
  "description": "Willa UI component library monorepo.",
  "scripts": {
    "prepare": "husky",
    "dev": "pnpm --filter willa-example run dev",
    "build": "pnpm --stream --filter willa-example run build",
    "dev:packages": "pnpm --parallel --stream --filter './packages/*' run dev",
    "build:packages": "pnpm -r --stream --filter './packages/*' run build",
    "preview": "pnpm --filter willa-example run preview",
    "publish:alpha": "auk publish --filter '@willa-ui/*' --filter willa --version alpha",
    "publish:beta": "auk publish --filter '@willa-ui/*' --filter willa --version beta",
    "publish:patch": "auk publish --filter '@willa-ui/*' --filter willa --version patch",
    "publish:minor": "auk publish --filter '@willa-ui/*' --filter willa --version minor",
    "publish:major": "auk publish --filter '@willa-ui/*' --filter willa --version major",
    "test": "pnpm -r --if-present test",
    "typecheck": "tsc -p . --noEmit --skipLibCheck",
    "format": "prettier --write --ignore-unknown --ignore-path .gitignore \"**/*.{js,mjs,cjs,ts,tsx,json,md,css,html}\""
  },
  "lint-staged": {
    "*.{js,mjs,cjs,ts,tsx,json,md,yaml,yml,css}": [
      "prettier --write --ignore-unknown"
    ]
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "husky": "catalog:",
    "auklet": "catalog:",
    "prettier": "catalog:",
    "lint-staged": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22",
    "pnpm": "=10.27.0"
  }
}
