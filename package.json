{
  "name": "willa-workspace",
  "version": "0.0.3-alpha.6e8a429",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "chentao.arthur",
  "description": "Willa UI component library monorepo.",
  "scripts": {
    "prepare": "husky",
    "dev": "pnpm --filter willa-example run dev",
    "build": "pnpm --stream --filter willa-example run build",
    "dev:packages": "pnpm --parallel --stream --filter './packages/*' run dev",
    "build:packages": "pnpm -r --stream --filter './packages/*' run build",
    "preview": "pnpm --filter willa-example run preview",
    "publish:alpha": "auk publish --filter '@willa-ui/*' --filter willa --version alpha --token env:NPM_TOKEN",
    "publish:beta": "auk publish --filter '@willa-ui/*' --filter willa --version beta --token env:NPM_TOKEN",
    "publish:patch": "auk publish --filter '@willa-ui/*' --filter willa --version patch --token env:NPM_TOKEN",
    "publish:minor": "auk publish --filter '@willa-ui/*' --filter willa --version minor --token env:NPM_TOKEN",
    "publish:major": "auk publish --filter '@willa-ui/*' --filter willa --version major --token env:NPM_TOKEN",
    "test": "pnpm -r --if-present test",
    "typecheck": "tsc -p . --noEmit --skipLibCheck",
    "format": "prettier --write --ignore-unknown --ignore-path .gitignore \"**/*.{js,mjs,cjs,ts,tsx,json,md,css,html}\""
  },
  "lint-staged": {
    "*.{js,mjs,cjs,ts,tsx,json,md,yaml,yml,css}": [
      "prettier --write --ignore-unknown"
    ]
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "husky": "catalog:",
    "auklet": "catalog:",
    "prettier": "catalog:",
    "lint-staged": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22",
    "pnpm": "=10.27.0"
  }
}
